export const PAGE_SIZE = 25
export const GRAHAM_FORMULA_MULTIPLE = 22.5

/** Default canvas height (px) for the income / ratio / Graham charts. */
export const CHART_HEIGHT = 320
/** Taller canvas height (px) for the interactive price chart. */
export const PRICE_CHART_HEIGHT = 400

/** Debounce (ms) before a type-ahead search fires a request. */
export const SEARCH_DEBOUNCE_MS = 300
/** Max ticker suggestions shown in the Compare type-ahead. */
export const COMPARE_AUTOCOMPLETE_LIMIT = 8

/** Width (px) of the screener's numeric filter fields. */
export const FILTER_FIELD_WIDTH = 110
/** Width (px) of the wider "Min margin" filter field. */
export const FILTER_FIELD_WIDTH_WIDE = 120
/** Width (px) of the free-text "Sector" filter field. */
export const SECTOR_FIELD_WIDTH = 180
