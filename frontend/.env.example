# Frontend env (Vite). Copy to `.env` (gitignored) and adjust.
# Only VITE_-prefixed vars are exposed to the app; this one is read by
# vite.config.ts to point the dev server's proxy at the backend.

# Where `npm run dev` forwards /api and /auth requests.
VITE_API_TARGET=http://localhost:8080
